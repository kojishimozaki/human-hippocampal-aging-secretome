# 事前登録 — 新監査 BLOCKER B-12 / B-13 / B-14（来歴の欠落）

作成 2026-08-27、**リポジトリに何かをコミットする前**に記録（CLAUDE.md 鉄則5）。

## 0. 対象と、着手前に確定させた事実

| # | 指摘 | 実測 |
|---|---|---|
| **B-12** | 一次 DE エンジンがどの Makefile ターゲットからも呼ばれない | `make` 全ターゲットを列挙し確認。`de_GSE268609_per_celltype.csv` の git 履歴は初回コミットのみ |
| **B-13** | reproduction 格下げの根拠（効果量マッチ帰無）が `results/` に無い | 監査ラン内のみ |
| **B-14** | 「60ヒット」の唯一の校正（置換帰無）が `results/` に無い | 原稿が引く `empirical p = 0.020 microglia…` の出典が前回監査ラン内のみ |

## 1. ★ B-12 で判明した事実（走らせて確認済み）

復元コマンドで一次 DE を再実行した結果、**委託済み表は現行コードでは完全再現できない**:

- 行数 99,544 → **99,543**。差は `LINC01238`(Oligo) **1 件**
- `LINC01238` は Oligo で総カウント **2**、非ゼロドナー **2/17**。
  現行の遺伝子フィルタ（`総和>=10 かつ 非ゼロ>=max(3, 0.5n)=8`）を**明確に落ちる**
- すなわち**委託済み表は現行より緩いフィルタで生成されている**
- `GOLGA8M`(Oligo) の log2FC が −0.305 → +0.077 に動く（1 遺伝子減ったことによる
  size factor / 独立フィルタの再計算の帰結）。**差>0.01 は 22,844 中この 1 件のみ**

### 結論に対する影響（確定）

- **凍結 60 ヒットは 60/60 が再現表に存在し、60/60 が `padj<0.1` を保つ**
- 凍結 60 の **log2FC 最大絶対差 4.3×10⁻⁵**
- 影響を受ける 2 遺伝子はいずれも `padj>0.9` の非有意遺伝子

## 2. 判定基準（作業前に固定）

- **G-B12**: Makefile ターゲットを追加し、その出力が委託済み表に対し
  **凍結 60 で `padj<0.1` 60/60・log2FC 最大差 < 1e-3** を満たすこと。
  満たさなければ**追加せず STOP して報告**する。
- **G-B13/14**: 監査ランからコピーする出力は、**原稿が実際に引用している数値を含むもの**に限る。
  コピー元・生成スクリプト・生成日を各ファイルの由来として記録する。
  **リポジトリ内で再計算できないものはその旨を明記**する（偽の再現性を作らない）。
- **共通**: 委託済みの一次 DE 表は**上書きしない**（`de_GSE325391` と同じ理由。
  再現版との差は記録し、置き換えるかはユーザーの判断とする）。

## 3. 環境変更（B-06 / doublet_recall の再実行に必要）

`sgz_r` に R パッケージ `celda`、`bio` に `scikit-image` を追加する。
CLAUDE.md 鉄則4（環境エラーで手法を黙って代替しない。**STOP して環境を直す**）に従う措置。
**追加後は `envs/*.yml` と `requirements_bio_pip.txt` の pin を更新し、変更を開示する。**

## 4. やらないこと

- 委託済み一次 DE 表の上書き
- 監査キット側のバグ 4 件の修正（原稿の主張に影響しない）
- 原稿の数値変更（本 run は来歴の整備であり、数値は変わらないことを確認済み）
